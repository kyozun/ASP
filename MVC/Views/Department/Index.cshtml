<div class="container">
    <div class="d-flex justify-content-between align-items-center">
        <h2 class="my-3 text-primary fw-bold">Department</h2>
        <a class="btn btn-success" asp-controller="Category" asp-action="Create">Create</a>
    </div>

    <div class="text-center">
        <table class="table">
            <thead>
            <tr>
                <th scope="col">ID</th>
                <th scope="col">Code</th>
                <th scope="col">Name</th>
                <th scope="col">Location</th>
                <th scope="col">Number of personals</th>
            </tr>
            </thead>
            <tbody>
            @* @foreach (var item in Model) *@
            @* { *@
            @*     <tr> *@
            @*         <td style="width: 20%">@item.Id</td> *@
            @*         <td style="width: 60%">@item.Name</td> *@
            @*         <td> *@
            @*             <a class="me-2 btn btn-primary px-3 py-2 text-white" asp-controller="Category" asp-action="Edit" asp-route-id="@item.Id">Edit</a> *@
            @*             <div class="d-inline-block"> *@
            @*                 *@
            @*             </div> *@
            @* *@
            @* *@
            @*         </td> *@
            @*     </tr> *@
            @* } *@
            </tbody>
        </table>
    </div>
</div>